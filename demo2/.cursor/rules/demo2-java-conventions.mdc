---
description: demo2 Java 通用编码约定（枚举命名、日志、Mapper SQL）
globs: demo2/src/main/java/**/*.java,demo2/src/main/resources/mapper/**/*.xml
alwaysApply: false
---

# demo2 Java 编码约定

## 枚举命名

- **所有 `enum` 类名必须以 `Enum` 结尾**
- 业务状态：`{Entity}StatusEnum`（如 `OrderStatusEnum`、`MemberStatusEnum`）
- 业务错误码：`{Module}ErrorCodeEnum`（如 `OrderErrorCodeEnum`、`MemberErrorCodeEnum`），实现 `ErrorCode`
- 框架通用错误码：`CommonErrorCodeEnum`

禁止：`OrderStatus`、`MemberErrorCode`、`CommonErrorCode` 等不带 `Enum` 后缀的枚举名。

## Mapper SQL

自定义 SQL **写在 `xxxMapper.xml`**，不要用 `@Select` / `@Insert` / `@Update` / `@Delete` 把 SQL 写在 Mapper 接口上。

- 接口只声明方法（可用 `@Param`）
- XML 放 `src/main/resources/mapper/{module}/{Entity}Mapper.xml`
- `namespace` 必须等于 Mapper 接口全限定名
- 仅 BaseMapper CRUD、无需自定义 SQL 时，可以不建 XML

## 日志

- **优先使用 Lombok `@Slf4j`**，不要手写 `private static final Logger log = LoggerFactory.getLogger(...)`
- 记录异常时，将 `Throwable` 作为 `log` 方法的**最后一个参数**，以便输出完整堆栈：

```java
@Slf4j
public class Example {
    void onBusinessFailure(BusinessException ex) {
        log.warn("Business exception: code={}, message={}", ex.getCode(), ex.getMessage(), ex);
    }

    void onUnexpectedFailure(Exception ex) {
        log.error("Unhandled exception", ex);
    }
}
```
